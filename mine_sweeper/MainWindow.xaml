<Window x:Class="mine_sweeper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mine_sweeper"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight" ContentRendered="Window_ContentRendered">

    <Border BorderBrush="Black" BorderThickness="4">
        <Grid Width="400" Height="400">
            <Canvas Name="area" ClipToBounds="True" Width="400" Height="400" />
            <Canvas Name="uiOverlay" ClipToBounds="True" Width="400" Height="400" IsHitTestVisible="True" />

            <!-- Inventory overlay -->
            <Grid Name="inventoryPanel" Visibility="Collapsed">
                <!-- Semi-transparent backdrop -->
                <Border Background="#CC000000" />

                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="130" />
                    </Grid.ColumnDefinitions>

                    <!-- Inventory grid (4x4) -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Inventory [Tab]" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" HorizontalAlignment="Center" />
                        <UniformGrid Name="inventoryGrid" Rows="4" Columns="4" Width="220" Height="220" />
                    </StackPanel>

                    <!-- Shop offers -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Upgrades" Foreground="Gold" FontSize="13" FontWeight="Bold" Margin="0,0,0,8" HorizontalAlignment="Center" />
                        <StackPanel Name="shopPanel" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
